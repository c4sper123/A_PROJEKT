<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.testnavigation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Requests.AppController"
        android:allowBackup="true"
        android:label="Zľavový portáľ"
        android:icon="@mipmap/ic_discount"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".LoginWindow"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".EditOfferScreen"
            android:label="@string/title_activity_edit_offer_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
